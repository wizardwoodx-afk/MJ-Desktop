# MJ 13.5.1 — Windows fix + cleanup (what differs from the zip)

> This tree is 13.5.1 **plus the fixes below**. The `v13.5.1` tag was re-cut
> on top of the shipped source so the app a pre-seed demo installs actually
> runs. Nothing else was touched: to verify, diff the tag against
> `MJ-Desktop-13.5.1.zip` — every difference is listed here, file by file.

## Code fixes

1. **`src-tauri/src/lib.rs` — updater plugin init removed.**
   `tauri_plugin_updater::Builder::new().build()` with `"plugins": {}` in
   `tauri.conf.json` is a fatal `PluginInitialization` error: every desktop
   build exited 101 before setup (no window, no data dir, no log — silent in
   release because the release profile sets `panic = "abort"`). Reproduced on
   the shipped tree with a console build before fixing. The updater has no
   endpoints, no pubkey, no artifacts and zero callers, so the init line goes;
   updates remain "download the new zip and reinstall".
2. **`src-tauri/Cargo.toml` — `tauri-plugin-updater = "2"` dependency removed**
   (orphaned by fix 1; `Cargo.lock` regenerated by the build).
3. **`probe/twoNodeAlign.test.ts` — leak-check made boundary-aware.**
   Bare `seen.includes(v)` false-fired on ISO `computedAt` timestamps
   (`...24.185Z` contains `4.1`) in ~1% of runs. The check now requires digit
   boundaries around each raw value — strictly stronger against real leaks,
   immune to timestamps. Proven 50/50 clean under repetition that false-fired
   50/50 before the fix.
4. **`verify/suites/*` + `verify/MANIFEST.json` — offline pack rebuilt** from
   the fixed probe via `tools/build-offline-verify.mjs` (the offlinePack
   freshness gate passes: rebuild == shipped).

## CI: Windows-only

- **`.github/workflows/release.yml`** — macOS/Linux matrix entries and the
  Linux system-deps step removed; signing env removed (nothing to sign
  without an updater); added a Rust-tests step and a **launch smoke step**
  (starts the built exe, fails the run if it exits within 25s — the exact
  gate whose absence shipped dead apps through 13.5.1).
- **`src-tauri/tauri.conf.json`** — bundle targets cut to `["nsis"]`;
  macOS/Linux bundle sections removed.

## Docs cleaned (stale or false statements removed)

- **`BUILD-NATIVE.md`** — Windows-11-only (mac/Linux outputs, toolchain
  sections and the webkit2gtk sandbox story removed); §0 rewritten (the full
  crate now compiles and tests green — the old "never compiled" claim was
  false); §3 verification table rewritten with what was actually verified.
- **`DESKTOP-NATIVE.md`** — Windows-11-only (§1 table, §4 bundles, PATH
  section states the login-shell fallback is unix-only).
- **`INSTALL-ON-LAPTOP.md`** — installer path corrected (nsis only, was a
  nonexistent msi); sanity checklist updated (13.5.1 + INK six, was 12.0 +
  retired names); updater section rewritten (was "wired but inert" — it was
  fatal); §3 rewritten (Rust compiles and tests here).
- **`docs/history/VERSION-CHANNELS.md`** — internal-build rows restored and
  extended through 13.5.0 (the zip shipped a stale copy).
- **`docs/history/MJ-11.14.5-POINTER.md`** — independent re-certification note
  restored (the zip shipped a stale copy).
- **`docs/history/MJ-13.5.1-POINTER.md`** — "Known issue" section recording
  the updater launch bug against the shipped tree.

## Deliberately NOT changed

- No visual/UI changes (13.5.1 froze the design system; theme + meridian
  probes pin exact values).
- No version bumps (all six manifests still agree on 13.5.1 — `versionDrift`
  passes unchanged).
- No behavior changes beyond the two fixes above.

# VH 16.5.0 — DRILL LINE, COMPLETED IDENTITY (2026-09-10)

> The 16.4 drill release plus the completed clean identity and spec-current
> MCP: legacy heritage names gone from the product surface, one version
> line, proof wire on vh-proof-receipt/2 with dual-accept readers.

The 16.5.0 upgrade notes ship in upstream `docs/history/VH-16.5-UPGRADE.md`
and the regenerated `verify/BUILD-INFO.txt`. This file is the discoverable
pointer in-repo.

## Gates

Re-run for 16.5.0: `tsc --noEmit` 0, **88/88 live suites**,
**87/87 offline bundles**, `vite build` 0, `npm audit --omit=dev` 0.
Counts unchanged from 16.4.1 (identity + spec-alignment release).

## Windows-fix note (this tree)

The shipped zip fails one gate on Windows: `mcpRouter`'s rebuild check
shells out to `node_modules/.bin/esbuild`, which exists but is not
executable on Windows (ENOENT) — fixed here (`esbuild.cmd` + shell flag).
The `loopBusFeed` detached-subscribe crash (every LoopPage mount) is also
fixed here (closure keeps the bus instance). Full record:
`docs/history/VH-16.5.0-WINDOWS-FIX.md`.

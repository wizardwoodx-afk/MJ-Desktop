# VH 16.4.1 — THE DRILL RELEASE (2026-09-10)

> The product proving itself on REAL missions: standard scenarios run on
> demand against fresh real git repos, the repo's OWN test command executed
> for real, through the REAL mission loop (governance arena, worktrees,
> signed cycle receipt) — with an honest-failure scenario that must report
> FAILED, never a fake pass. Plus the MCP capability router (20 governed
> tools) and the gated reversible meta-loop inherited from 16.2–16.3.

The 16.4.1 upgrade notes ship in upstream `docs/history/VH-16.4-UPGRADE.md`
and the regenerated `verify/BUILD-INFO.txt`. This file is the discoverable
pointer in-repo.

## What landed

- **The drill** (`src/vouch/engine/drill.ts`, `DrillPage`): guard / maths /
  impossible scenarios; vouched drill reports with reproducible attestation
  digests; failed scenarios keep forensic git repos. Pinned by `drill`
  (suite #88).
- **MCP router** (16.2): 20 governed tools over JSON-RPC stdio, byte-pinned
  engine bundle (`tools/mcp-engine.mjs` + `.sha256`). Pinned by `mcpRouter`.
- **Meta-loop** (16.3): product proposes changes to its own control plane
  (risk-tier tighten-only, preferences), every decision vouched, reverts
  gated + terminal. Pinned by `metaLoop`.

## Gates

Re-run for 16.4.1: `tsc --noEmit` 0, **88/88 live suites**,
**87/87 offline bundles**, `vite build` 0, `npm audit --omit=dev` 0.
16.0.0 / 16.1.0 were internal (see VERSION-CHANNELS.md).

## Windows-fix note (this tree)

The shipped zip fails one gate on Windows: `mcpRouter`'s rebuild check
shells out to `node_modules/.bin/esbuild`, which exists but is not
executable on Windows (ENOENT) — fixed here (`esbuild.cmd` + shell flag).
The `loopBusFeed` detached-subscribe crash (every LoopPage mount) is also
fixed here (closure keeps the bus instance). The `twoNodeAlign` timestamp
flake fix was already absorbed upstream. Full record:
`docs/history/VH-16.4.1-WINDOWS-FIX.md`.

# Changelog

All notable changes — one line per release, with the bug that was fixed.

| Version | What changed |
|---|---|
| **11.9.7** | Provenance-polish release (review fix from the 9.95/10 assessment). Module headers that claimed a single version now state their FULL lineage — `app/fuzzy.ts` (subsequence core 11.9.5 → label-first `paletteScore` 11.9.6), `panels/CommandPalette.tsx`, `probe/palette.test.ts` — while historical release markers in comments stay untouched, because they are facts, not stale identity. The rule this pins: a header names what shipped WHEN. No behavior change; re-certified end-to-end under Node v22.23.2 (fresh npm ci, tsc 0, 47/47 live, pack rebuild, 46/46 offline, vite build 0). |
| **11.9.6** | Certification + review-fix release. (1) **Palette ranking is label-first** (review fix): `paletteScore` lifts every visible-label hit above any category hit — a group name can still surface a command the label alone misses, but damped ×0.35, never out-ranking real label matches; the old concatenated group+label search text is gone; new probe assertions pin the bucket separation. (2) **Node-22 re-certification** (review fix): the whole matrix was re-run under Node v22.23.2 LTS — fresh `npm ci`, `tsc --noEmit`, 47/47 live suites, pack rebuild and 46/46 offline gate — and BUILD-INFO now states the certifying runtime honestly, including the one thing this environment cannot do (compile the Rust/Tauri shell — no cargo here; the typed TS↔Rust command table stays probed). Gates: typecheck 0, npm test 47/47, offline 46/46, vite build 0 — all under Node 22. |
| **11.9.5** | The craft release — fix the fundamentals, then the features. **Major fixers:** wires now connect properly: drag-from-output completes on release inside a 30px port magnet (measured anchors, never a stale model), click-click wiring survives, invalid drops speak (`connectRefusal` names the exact rule: self-wire / ghost port / type mismatch / occupied input / would-loop) instead of failing silently, ghost wire shows a snap ring, wire colors fall back to theme tokens. **Three major features, each researched:** (1) **Preflight lint** (`graph/lint.ts`) — "ESLint for the graph" after FlowLint + n8n-workflow-validator: DANGLING_WIRE / TYPE_MISMATCH / DUPLICATE_WIRE / CYCLE / REQUIRED_INPUT_OPEN are errors that refuse the run; ORPHAN_NODE / AGENT_UNPROVISIONED are warns that never do; PreflightPanel + live statusbar chip, click-to-focus rows. (2) **Command palette, Raycast-grade** (`panels/CommandPalette.tsx` + `app/fuzzy.ts`) — real fuzzy subsequence ranking (exact > prefix > word-boundary > run), grouped results, jump-to-node entries, recents memory, full keyboard nav. (3) **Checkpoints** (`graph/checkpoints.ts`) — LangGraph-shaped named time travel for the design graph: deep-copy snapshots, structural-equality dedupe, 25-entry cap, restore keeps your viewport AND rides the undo stack. Plus: **Assist recut** (welcome state with suggestion cards, user bubbles, bot answer cards with via-chips + inspector action), the thirteenth palette **cyanotype** — five-color rare (Prussian ground `#0b1524`, paper ink `#e9eef7`, exposure-cyan signal `#8fc6ff`, madder error, viridian alive) joined to all 118 shared selector groups, and continued redesign-layer UI/UX. Live 47/47, offline 46/46 (node v20.20.2), tsc 0, vite build 0. |
| **11.9.4 (Redesign)** | Differentiation + review-fix release. **(1) Differentiator — Proof Receipts** (`mission/receipts.ts`, researched against the Aug-2026 EU AI Act tamper-evidence enforcement and IETF AAT/SCITT receipt work): every mission can export a SHA-256 hash-chained, sealed receipt as SIEM-friendly JSONL; verification needs zero MJ state (re-canonicalize → re-hash chain → re-check seal); recursive key-sorted canonicalization so data tampering is provably caught; UI on TeamsPage (export + external verify); probed end-to-end by new suite #44 including tamper and JSONL roundtrip. **(2) Review fix** — elastic seats inherit a harness the team already runs (`inheritHarness`: reviewer ← read-only seats, debugger ← writers, `llm` as the zero-binary fallback); no hardcoded vendor default survives; asserted in teamPower. **(3) Complete structural redesign** — new `styles/redesign.css` layer loaded after mj.css (tokens-only, so all 12 themes inherit): Space Grotesk display voice, 10/8px radius scale, layered hairline elevation, rail notch markers, pill canvas toolbar, vignette canvas, ring ports with spring hover, drawer/inspector slide-ins, mature decel/spring keyframe vocabulary with per-card stagger, toast springs, focus rings, restyled scrollbars — all yielded to `prefers-reduced-motion`. Live 44/44, offline 43/43 (node v20.20.2), tsc 0, vite build 0. |
| **11.9.4 (Commercial)** | Commercial-readiness pass, researched against 2026 PLG/pricing benchmarks: **licensing engine** (`mission/licensing.ts`) — personal/trial/pro editions, 14-day no-card trial, offline HMAC-signed Pro keys issued by founder tooling (`tools/issue-license.mjs`), soft gate declared as such (honesty rule), Pro unlocks only AUTONOMOUS evolution + elastic caps >5; **first-run onboarding** (`panels/Onboarding.tsx`) to the time-to-value bar — theme default, labelled sample team on the in-process harness, skip path, never blocks; **License card** in Settings; autonomy settlement reports the governing edition; new `licensing` probe suite (43/43, pack 42); README counts + dual-license note updated. Investor memo in `docs/FUNDING.md`, deck in `MJ-Pitch-Deck.pptx`. |
| **11.9.4 (Major+)** | Review-driven integration pass: the three autonomy engines are now **wired into real execution** — `prepareAutonomy()` picks bandit arms before each team run, `executeTeam` modulates the run for real (review/check arms become briefing lines agents read, `exec:serial` flattens the wave plan, Researcher briefings carry live web-evidence triples with honest provider-failure notes) and echoes the executed arms in the report; TeamsPage settles every run via `settleAutonomyAfterRun` (bandit posteriors update on exactly the arms executed, elastic policy acts mode-gated: AUTONOMOUS applies the seat, SUGGEST logs a suggestion) into the shared `autonomyStore` the Evolution page now reads live (storage-event refresh + settled-run log). Review fixes: bandit seeding informs the prior **without fake pulls**; web providers carry `sourceKind` (GitHub repository search labelled primary, Wikipedia/HN honestly secondary); `verify/BUILD-INFO.txt` regenerated for 41/41 and **pinned by a new offlinePack gate** so provenance can never drift again. |
| **11.9.4 (Major)** | The Autonomy Upgrade, frontend/TS-only: **Web Evidence engine** (`mission/webSearch.ts`) — Researcher gains live primary-source search (keyless CORS-open Wikipedia/HN/GitHub + optional self-hosted SearXNG / BYO-key Brave), measured confidence triples, honest per-provider failure reporting; **Elastic Seats** (`mission/elasticSeats.ts`) — pure scaling policy (debugger on fail-streak, reviewer on review pressure, capped scale-in, praised seats untouchable); **Bandit-Routed Evolution** (`mission/evolutionBandit.ts`) — UCB1 router over review/exec/check strategy arms with a stagnation-armed structural-jump arm (RecHarness/AEL 2026), simulated runs logged as experience but never moving posteriors, seedable from the measured seat ledger, Experiment-Skill digest; **Autonomy card** on the Evolution page; two new probe suites (42/42, pack re-cut at 41); `versionDrift` §6 now also pins suite + bundle counts; README numbers updated. |
| **11.9.4 (Fixed)** | UI/UX rework, frontend-only: twelfth palette **wabi** becomes the default — five-color minimal (charcoal `#141414` ground, washi `#efeeea` ink, kiln `#e07a5f` signal, moss `#81b29a` alive, berry `#d1495b` error) with every secondary tone derived via `color-mix`, joined to all 117 shared selector groups; motion system added — animated boot splash in `index.html`, staggered node entrance, wire draw-in, midpoint pop, port hover scale, keyed page transitions with a better curve, spinning Suspense fallback, press micro-interactions — all transform/opacity-only and all yielded to `prefers-reduced-motion`. Theme probe updated deliberately (twelve-palette set, wabi §3.6, motion checks); offline pack re-cut. Also: README Tauri-command count corrected 92→94 and pinned by a new `versionDrift` §6 guard. |
| **11.9** | NTH visual system + canvas integrity: de-vendored the external agent (hermes removed, ~12 MB), added the `nth` default palette (obsidian + electro-violet volt + plasma pulse — 5 rare colours, no brown), volt-junction icons, percussive entrance/press motion, layout alignment; and fixed the canvas — wires now re-measure onto ports after a drag/drop (port geometry uses rendered bounding boxes), nodes own their stacking context so an overlapping node never exposes a behind node's ports, ports sit inside the card edge, minimap repositioned, node sizing tuned. Version 11.9 across all manifests. |
| **6.0** | Agent runtime × identities × reusable teams |
| **7.0** | Real verification + git layer, replay/evals, Proof page |
| **8.0** | Version metadata + reviewer-visibility fix |
| **9.0** | Reviewer snapshot against writers' branches |
| **10.0** | Replay, evals, git, Proof page + `nothing` theme pass |
| **11.3** | Inscription pass — tokens, real fonts, mechanical motion |
| **11.4** | Teams loop audit (praise-drain + arms suppression), palettes, signal diet |
| **11.5** | Meridian — icon grammar, node contracts, Assist redesign |
| **11.6** | Connector — 25-harness registry, custom harnesses, Connect tab |
| **11.7.0** | Fixed vacuous harness gate (`ok()` args reversed since 11.6) — 5 latent defects surfaced |
| **11.7.1** | Offline gate (`verify/run.mjs` 39 bundles + `MANIFEST` + suite #40 freshness) + 4 harnesses (21→25) |
| **11.8.0** | Turn-limit truth — `withTurnLimit()` now capability-driven, claude `--max-turns` docs-grade |
| **11.8.1** | Env-hardening — EACCES/EPERM/ENOEXEC → UNMEASURED, exit-code-first verdict |
| **11.8.4** | Vendoring — ships `vendor/*` (hermes-agent pruned, mcp-servers, mcp-github, evolution-service) + browser stubs (`fs/os/path` → honest throws) |
| **11.8.5** | CI-currency (ubuntu-24.04/macos-15/Node22, 40-suite gate) + typed `MjCommands` registry, zero `as never` in `src/` |
| **11.8.5(i)** | Deep type pass — `PersistedMissionState` concrete types, `methodFor`/`composeAssignment` narrowed, `workflowGet: WorkflowRecord` |
| **11.8.7** | De-vendored the external agent — removed `vendor/hermes-agent` + `vendor/hermes-agent-self-evolution` (~12 MB / 633 files). Skill contract, parsing, and the evolution fitness/constraints engine are now MJ's own TypeScript. `NOTICE`/`VENDOR.md` updated; only the two runtime MCP server sets remain vendored. |
| **11.9.1** | Release-packaging + provenance: the previous 11.9.0 archive shipped `verify/suites/` empty (a 0/0 vacuous green gate) and a stale docs/`BUILD-INFO.txt` (11.8.5) and `DEEP-DEBUG-REPORT.md` title. 11.9.1 regenerates the offline pack from the 11.9.1 source (39 bundles), rewrites `BUILD-INFO.txt` for 11.9.1, corrects the report title and reconciles its stale "still open" type-hole section (those are closed via the typed `MjCommands` registry), and fixes README vendor/history drift. All gates green: `tsc` 0, 40/40 live probes, 39/39 offline, `vite build` 0. |
| **11.9.4** | UI/UX redesign — frontend-only, backend untouched. Re-cut the flagship on the v3 "Domain Color Atlas" identity: carbon ground `#0e0f0e`, cream ink `#f3eee3`, brass accent `#b8a17b`, verdigris "alive" pulse `#7ba58e`, functional oxide for errors. Added a serif display voice (`--font-serif`) for the wordmark/hero and every page title; redrew the whole icon set on the FOUNDRY grammar (heavier 1.9 engraved stroke + brass facet chip instead of the round LED dot); gave the active rail item a brass "plate" marker. All 12 flagship tokens re-asserted and the theme/meridian probes updated deliberately. Verified 40/40 probes, offline 39/39, live render warm/brass. |
| **11.9.3** | UI/UX fix — the NTH theme now actually renders. Root cause: the `:root` fallback palette in `mj.css` (specificity `0,1,0`) sat *after* the `@import`ed `themes.css`, so it won the cascade tie against every `[data-theme="…"]` block (also `0,1,0`). Every theme silently rendered the inscribed red/black instead of its own palette. Fix: pin the fallback to zero specificity (`:where(:root)`) so theme blocks always win. The whole app now renders NTH obsidian `#05060b` + electro-violet volt `#7c5cff` (verified: all 12 NTH tokens match). |
| **11.9.2** | Verification toolchain + provenance: the offline pack is now built and verified under **Node 22** (`v22.23.2`) — the same supported LTS line CI pins — instead of EOL **Node 20** (which reached end-of-life 2026-04-30 and must not certify a release). `verify/BUILD-INFO.txt` records Node 22 and notes Node 20 is EOL; `docs/VERIFICATION.md` tier line updated to 11.9.2. All 39 bundles rebuilt from the 11.9.2 source; independent SHA-256 recompute 39/39 match; gate under Node 22: `tsc` 0, 40/40 live probes (incl. `offlinePack` freshness + `versionDrift`), 39/39 offline, `vite build` 0. |

Full per-release notes live in [docs/history/](docs/history/).

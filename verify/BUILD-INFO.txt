MJ 11.14.10 — offline verification pack provenance
===================================================

Release identity: MJ 11.14.10 — "the Consolidation Release" (one product,
one spine: the app's destinations were two drift-prone ad-hoc arrays with
legacy per-release labels; they are now ONE source of truth,
src/app/nav.ts, shaped as the product lifecycle Overview -> Build -> Run ->
Verify -> Learn -> System, where every destination carries a stable
PageKind key, a purpose-clear public label (Workflows, Mission Control,
Connectors — Canvas/Control/MCP retired), an icon and a one-line purpose
shown as the sidebar tooltip; App.tsx renders ONLY from the map;
probe/navAlign (30 assertions) pins the consolidation: no orphan pages,
canonical lifecycle order, public-vocabulary labels, no forked nav in App,
Home hero aligned to the pillars; docs/INFORMATION-ARCHITECTURE.md ships
the model + rename table + researched 2026 IA principles). Numbering note:
11.14.9 was intentionally skipped — 11.14.10 ships directly after 11.14.8
(the established skip pattern).

This document is regenerated FROM THIS TREE at certification time. It names no
other source tree and no older release as the pack's origin: the bundles in
verify/suites were built from the probe sources in this same checkout, and the
counts below were read from this same certification run.

Toolchain (certifying runtime)
  node          v22.23.2  (Node 22 LTS "Jod" — the same runtime CI certifies
                           on; every gate below executed under this runtime:
                           npm ci, tsc, live suites, pack rebuild, offline
                           gate, vite build)
  esbuild       0.25.12
  MJ            11.14.10 (package.json + package-lock.json / version.ts /
                tauri.conf.json / Cargo.toml + Cargo.lock / LICENSE)

Pack contents
  verify/suites/*.mjs   72 self-contained bundles — every probe suite except
                        probe/offlinePack.test.ts (the pack's own freshness
                        gate, which runs live only)
  verify/MANIFEST.json  sha256 per bundle + the MJ and esbuild versions that
                        built it
  verify/run.mjs        offline runner: no network, no installs, no MJ state

Live certification (this tree)
  npm test              ALL PROBES SUMMARY: 73 passed, 0 failed.
                        (73 probe suites; includes navAlign 30/30
                        consolidation assertions, arenaWired 15/15 mission-
                        path wiring assertions, arenaGate 22/22 hostile-
                        scenario assertions, codeHygiene 13/13 hygiene
                        assertions, selfEvolve 52/52 causal experiment
                        assertions, governanceAlign 31/31 envelope +
                        principal + ledger-enforcement assertions,
                        capabilityAlign 26/26 capability-not-data +
                        Privacy Guard + durability assertions,
                        guardrailAlign 15/15 code-level guardrail assertions,
                        twoNodeAlign 13/13 two-machine-proof assertions,
                        patternAlign 9/9 pattern-provenance assertions,
                        egressAlign 8/8 egress-authority + receipt
                        assertions)

Offline gate (this pack, this tree)
  node verify/run.mjs   OFFLINE VERIFY SUMMARY: 72 passed, 0 failed., exit 0
                        (node v22.23.2)

vite build              clean, 0 errors.
tsc --noEmit            0 errors.

11.14.10 — what changed over 11.14.8
------------------------------------
  1. THE NAVIGATION SINGLE SOURCE OF TRUTH (src/app/nav.ts). Every
     destination: stable key (PageKind), purpose-clear public label, icon,
     one-line purpose. Groups are the six lifecycle stages in canonical
     order: Overview, Build, Run, Verify, Learn, System.
  2. App.tsx RENDERS ONLY FROM THE MAP. The two ad-hoc local arrays
     (NAV, NAV_GROUPS) are deleted; keys, page components, tab behaviour
     and Ctrl+B collapse are unchanged. Sidebar tooltips now state each
     destination's purpose.
  3. probe/navAlign.test.ts (30 assertions): every PageKind has exactly one
     nav entry (no orphan pages), the groups are the canonical lifecycle in
     order, labels match the public vocabulary (Canvas/Control/MCP retired;
     Workflows / Mission Control / Connectors), App cannot fork its own nav
     again, and the Home hero speaks the same pillar language.
  4. docs/INFORMATION-ARCHITECTURE.md: the model — spine, six-group
     destination table with purposes, legacy -> public rename table, the
     researched 2026 IA principles applied, and the rule for future
     releases (a feature belongs to a destination; a destination opens only
     when map + switch + probe move together).

Counts: probe suites 72 -> 73; offline bundles 71 -> 72.
Re-certified under node v22.23.2 (tsc 0, 73/73 live incl. offlinePack
freshness, 72/72 offline, vite 0).

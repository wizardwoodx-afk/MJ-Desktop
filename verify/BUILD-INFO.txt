MJ 11.8.1 — offline verification pack provenance
=================================================

Built and certified by the release session, 2026-09-03.

Toolchain
  node          v20.20.2 (linux x64)
  esbuild       0.25.12
  MJ            11.8.1 (package.json + package-lock.json / version.ts /
                tauri.conf.json / Cargo.toml / Cargo.lock)

The pack
  verify/suites/*.mjs   39 self-contained bundles — every probe suite except
                        probe/offlinePack.test.ts (the pack's own freshness gate,
                        which needs the dev toolchain and is therefore not packed)
  verify/MANIFEST.json  sha256 per bundle + the MJ and esbuild versions that built it
  verify/run.mjs        zero-dependency runner (node: builtins only)

Gates run on the release tree (fresh npm ci — the 7th review's clean-env requirement)
  tsc --noEmit          exit 0
  npm test              ALL PROBES SUMMARY: 40 passed, 0 failed.
                        (suite #39 harnesses: 270 real assertions incl. §10
                        registry<->policy turn-flag iff-agreement; suite #40: 15;
                        checkRunner 45 — first direct testRunCheck unit tests;
                        sandbox 26 — V11.8.1 unusable-wrapper regression)
  vite build            ✓ built (exit 0)

Offline gate (reviewer simulation)
  Environment: git archive extract — no node_modules, no .git, plain node
  Command:     node verify/run.mjs
  Result:      OFFLINE VERIFY SUMMARY: 39 passed, 0 failed. (node v20.20.2), exit 0

Mutation tests (fail loudly when flipped; green when reverted)
  1. HARNESSES.length === 25 -> 26          suite #39: 239/1, count check named
  2. one tampered byte in a bundle          suite #40: 13/2, the bundle named
     (byte-identity + manifest hash)
  3. SOURCE-LEVEL (V11.8.0): claude registry maxTurns argv -> null
                                             suite #39: 268/2 — the docs-grade
                                             restoration check and the
                                             before-the-prompt composition check
                                             both fail; revert -> green. Proves the
                                             gate detects real registry drift, not
                                             just probe-string drift.
  4. SOURCE-LEVEL (V11.8.1-A): WRAPPER_UNAVAILABLE reverted to ENOENT-only
                                             (the 11.8.0 code). sandbox probe:
                                             21 passed, 5 failed — §2.5's synthetic
                                             EACCES fixture (a present, readable,
                                             non-executable wrapper) reproduces the
                                             7th review's defect verbatim: the
                                             failure detail shows enforced:true,
                                             failedAsExpected:true on a canary that
                                             never ran. Revert -> 26/0. Proves a
                                             present-but-unexecutable wrapper can
                                             never again certify enforcement.
  5. SOURCE-LEVEL (V11.8.1-B): testRunCheck's verdict restored to the 11.8.0
                                             any-match regex (fail|error|Traceback...
                                             anywhere in the output). checkRunner
                                             probe: 41 passed, 4 failed — including
                                             the reviewer's exact case (exit 0,
                                             "1 passed", ambient Traceback ->
                                             falsely FAILED) and the "0 failed"
                                             count false-fail. Revert -> 45/0.
                                             Proves ambient stderr noise can never
                                             veto a measured pass.

V11.8.1 in one line
  The 7th review ran the shipped offline gate in its own environment (37/2);
  both failures were environment-classification defects, both are fixed, and
  both are now pinned by deterministic regressions (probe fixtures, not
  environment luck): an unspawnable wrapper is UNMEASURED, and the exit code
  is the verdict.

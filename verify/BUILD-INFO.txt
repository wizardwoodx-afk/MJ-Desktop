MJ 11.10.0 — offline verification pack provenance
==================================================

Toolchain (certifying runtime)
  node          v22.23.2  (Node 22 LTS "Jod" — every gate below was executed
                           under this runtime: fresh npm ci, tsc, live suites,
                           pack rebuild, offline gate, vite build)
  esbuild       0.25.12
  MJ            11.10.0 (package.json + package-lock.json / version.ts /
                tauri.conf.json / Cargo.toml / Cargo.lock / LICENSE)

Honest scope
  Rust/Tauri shell compilation was NOT re-run in the certifying environment
  (no cargo toolchain there). The typed TS↔Rust command boundary is covered
  by the probe suites (controlPlane) and the version manifests are pinned by
  versionDrift; the Rust sources themselves are unchanged since 11.9.4.

The pack
  verify/suites/*.mjs   52 self-contained bundles — every probe suite except
                        probe/offlinePack.test.ts (the pack's own freshness gate,
                        which needs the dev toolchain and is therefore not packed)
  verify/MANIFEST.json  sha256 per bundle + the MJ and esbuild versions that built it
  verify/run.mjs        zero-dependency runner (node: builtins only)

Gates run on this tree (fresh npm ci, Node v22.23.2)
  tsc --noEmit          exit 0
  npm test              ALL PROBES SUMMARY: 53 passed, 0 failed.
                        (53 probe suites; harnesses 270 · webSearch · teamPower ·
                        receipts · licensing · preflight · checkpoints · palette · layout ·
                        verifyGate · roleBoard · receiptVault · fleet · NEW 11.10: mergeGate
                        incl. the label-first ranking checks · layered-layout engine · offlinePack)
  vite build            exit 0

Offline gate
  Command:     node verify/run.mjs
  Result:      OFFLINE VERIFY SUMMARY: 52 passed, 0 failed., exit 0 (node v22.23.2)

  This pack was rebuilt from the 11.10.0 source under Node 22 immediately before
  the run above, and suite #53 (offlinePack) confirms every bundle is
  byte-identical to a fresh rebuild AND that this file names the current
  bundle/suite counts — BUILD-INFO drift is now a failing gate, not a review
  finding.

11.10.0 — what the new suite pins
  #53 mergeGate      the adversarial gate OWNS the merge path: a REAL executeTeam over a
                     real git repository (stubbed CLIs) — cross-harness review → PASS,
                     evidence bound to the snapshot sha, merge allowed; same-harness
                     review → BLOCKED + merge blocked + override required (STRICT);
                     ADVISORY allows with the failure recorded; rejecting verifier blocks
                     the merge. Plus verifyGate +8 assertions (snapshot evidence binding,
                     enforceMergeGate) and fleet +1 (isDecided break-glass check).
